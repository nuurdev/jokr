import fetchProfileRoute from './fetch';
import loginRoute from './login';
import registerRoute from './register';

const authRoutes = [fetchProfileRoute, loginRoute, registerRoute];

export default authRoutes;
